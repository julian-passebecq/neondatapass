[project]
name = "neondatapass"
version = "0.1.0"
description = "Neon control-plane adapter for Datapass Workbench"
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2,<4",
]

[tool.ruff]
line-length = 100
